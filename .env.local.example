# Copy to .env.local for local development.
# Base URL of the Recon FastAPI backend, including the /api/v1 prefix —
# same backend recon-frontend talks to, just the /broker/* routes.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
